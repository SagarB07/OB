<?xml version="1.0"?>
  <database name="TRIGGER ASV_FECHA_PEDIDO_TRG">
    <trigger name="ASV_FECHA_PEDIDO_TRG" table="ASV_PEDIDO_INVENTARIO" fires="before" insert="true" update="true" delete="false" foreach="row">
      <body><![CDATA[
  
BEGIN

    IF date(:NEW.fecha) >= date(NOW()) THEN
        RETURN NEW;
    ELSE 
	RAISE NO_DATA_FOUND;
    END IF;

END ASV_FECHA_PEDIDO_TRG
]]></body>
    </trigger>
  </database>
